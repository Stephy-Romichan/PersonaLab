# Persona Lab dependencies
#
# Mock mode (M1/M2) needs NOTHING here — the notebook runs on the stdlib alone.
# These are required only at M3 when USE_MOCKS = False, plus the UI (M2).

# --- LLM SDKs (M3: live wiring) ---
openai>=1.40.0
google-genai>=0.3.0
anthropic>=0.39.0

# --- JSON schema / validation (Moderator) ---
pydantic>=2.7.0

# --- UI (M2) ---
streamlit>=1.36.0

# --- Env loading ---
python-dotenv>=1.0.0
